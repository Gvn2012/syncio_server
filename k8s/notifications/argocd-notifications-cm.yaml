apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-notifications-cm
  namespace: argocd
  labels:
    app.kubernetes.io/component: notifications-controller
    app.kubernetes.io/name: argocd-notifications-controller
    app.kubernetes.io/part-of: argocd
data:
  # Telegram Service Configuration
  service.telegram: |
    token: $telegram-token

  # Minimalist Templates for Troubleshooting
  template.sync-succeeded: |
    message: "✅ ArgoCD: {{.app.metadata.name}} Synced"
  
  template.sync-failed: |
    message: "❌ ArgoCD: {{.app.metadata.name}} Failed"

  template.sync-running: |
    message: "🔄 ArgoCD: {{.app.metadata.name}} Syncing"

  # Notification Triggers
  trigger.on-sync-succeeded: |
    - when: app.status.sync.status == 'Synced' and app.status.health.status == 'Healthy'
      send: [sync-succeeded]
  
  trigger.on-sync-failed: |
    - when: app.status.sync.status == 'Failed'
      send: [sync-failed]

  trigger.on-sync-running: |
    - when: app.status.sync.status == 'Syncing'
      send: [sync-running]
