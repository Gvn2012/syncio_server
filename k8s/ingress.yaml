apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: syncio-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: syncio-gateway
                port:
                  number: 80
